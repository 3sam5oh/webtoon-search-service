# 모든 파일에 대해 자동 줄 바꿈 처리를 설정합니다.
* text=auto

# 쉘 스크립트 파일은 항상 LF(Line Feed)로 처리합니다.
*.sh text eol=lfl=lf

# Dockerfile도 LF로 처리합니다.
Dockerfile text eol=lf

# 기타 설정 파일들도 LF로 처리합니다.
*.yml text eol=lf
*.yaml text eol=lf
*.json text eol=lf
